<section>
  <h1><%= flickr_user_id %>'s Photos</h1>
  <div>
    <% photos.each do |photo| %>
      <p><%= photo[:url] %></p>
      <img src="<%= photo[:url] %>" alt="photo" %>
    <% end %>
  </div>
</section>